apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-cati-deployment
  labels:
    app: postgres-cati
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-cati
  template:  #template - configuration for pod
    metadata:
      labels:
        app: postgres-cati
    spec:
      containers: #list of conrtainers (pods) - mostly one 
      - name: postgres-cati
        image: postgres:10.20-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            configMapKeyRef: 
              name: postgres-cati-config
              key: postgres-cati-pass
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef: 
              name: postgres-cati-config
              key: postgres-cati-user
        - name: POSTGRES_PORT
          value: "5432"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-cati-service ## endpoint to access postgres
spec:
  type: NodePort
  selector:
    app: postgres-cati
  ports:
    - protocol: TCP
      port: 5432 #port of service
      targetPort: 5432 #port of the pods (postgres port in container)
      nodePort: 30080 #access pods on outside (30000 32767)